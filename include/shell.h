/* Interactive kernel shell. */
#ifndef ZERT_SHELL_H
#define ZERT_SHELL_H

#include "types.h"

/* Runs the read-eval-print loop forever (never returns). */
void shell_run(void);

#endif /* ZERT_SHELL_H */
